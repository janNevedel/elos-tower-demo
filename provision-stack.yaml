- hosts: localhost
  connection: local
  gather_facts: False

  tasks:
    - name: Provision a set of instances
      ec2:
         key_name: jan.nevedel-elos
         group: mmb-demo
         instance_type: t2.micro
         image: ami-7c491f05
         wait: true
         exact_count: 3
         count_tag:
            Name: Demo
         instance_tags:
            Name: Demo
      register: ec2

